# Force npm to install optional dependencies, including platform-specific
# native bindings (e.g. @tailwindcss/oxide-linux-x64-gnu on Linux build
# servers). Works around https://github.com/npm/cli/issues/4828 where
# `npm ci` skips optional deps when the lockfile was generated on a
# different OS/arch than the install target.
include=optional
